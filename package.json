{
  "name": "eleicoesimarui2016",
  "version": "0.0.1",
  "devDependencies": {
    "axios": "^0.13.1",
    "react-scripts": "0.2.1"
  },
  "dependencies": {
    "react": "^15.2.1",
    "react-dom": "^15.2.1",
    "moment": "^2.14.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && node cname.js",
    "eject": "react-scripts eject",
    "gh-pages": "git checkout -B gh-pages && git add -f build && git commit -am \"Rebuild website\" && git push origin :gh-pages && git subtree push --prefix build origin gh-pages && git checkout -",
    "deploy": "npm run build && npm run gh-pages"
  },
  "eslintConfig": {
    "extends": "./node_modules/react-scripts/config/eslint.js"
  }
}
